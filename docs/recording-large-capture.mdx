---
id: recording-large-capture
title: Large Capture Areas
sidebar_label: Large Capture Areas
---

Best practices for recording activities that require a large capture area, such as running, change of direction, and triple hops.

:::tip
Before finalizing your setup, have the participant perform the activity while you monitor each camera's field of view, and adjust camera orientation to ensure the full movement is visible from all cameras.
:::

## 1. Recording Setup

Ensure cameras are correctly positioned to capture movement spanning a large area.

### Set up the cameras

- Mount the 2 iPhones/iPads on tripods in **horizontal/landscape mode**.
  - *If using 3 phones, position the front-facing one in **vertical/portrait mode**.*
- Follow the setup diagram below for ideal tripod positions and field of view.
- Open the Model Health app on all phones and adjust positions until the **full capture area is visible** in every camera view.

<div style={{textAlign: 'center', margin: '1.5rem 0'}}>
  <img src="/img/docs/recording-large-capture/Setup_Large-capture.png" alt="Large capture area setup diagram" style={{width: '50%'}} />
  <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>
    Two sagittal cameras (horizontal) positioned ~3–4 m from the change of direction line, with an optional front-facing camera (vertical) ~4-5 m behind the end of the movement area (e.g., the change of direction point or triple hop landing).
  </p>
</div>

### Verify the field of view

:::tip Tips
* Use cones to mark the capture area boundaries and adjust the cameras until every cone is visible in each view.
* Have the participant perform the activity while you monitor each camera's field of view
:::

<div style={{textAlign: 'center', margin: '1.5rem 0'}}>
  <img src="/img/docs/recording-large-capture/Large_volume_capture-left camera landscape.png" alt="Large_volume_capture-left camera landscape" style={{width: '50%'}} />
  <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>
    Left camera - Verify field of view for optimal volume capture
  </p>
</div>

<div style={{textAlign: 'center', margin: '1.5rem 0'}}>
  <img src="/img/docs/recording-large-capture/Large_volume_capture-right camera landscape.png" alt="Large_volume_capture-left camera landscape" style={{width: '50%'}} />
  <p style={{fontSize: '0.8rem', color: 'var(--ifm-color-secondary-darkest)', marginTop: '0.4rem'}}>
    Right camera - Verify field of view for optimal volume capture
  </p>
</div>

## 2. Setup calibration

- Place the checkerboard **strictly perpendicular to the ground**, with its long edge parallel to the ground, visible by all phones, approximately **3–5 m from each phone**.
- Once ready, press **"Calibrate"**.

Once calibration is done, the cherckerboard does not need to stay in view

:::tip
If calibration fails, try moving the checkerboard slightly closer to the phones or adjusting the lighting conditions.
:::

## 3. Static pose

- Have your participant stand in static pose around the position where you positioned the checkerboard, foot straight and arm slightly detached from the body.